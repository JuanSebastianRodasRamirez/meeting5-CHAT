services:
  - type: web
    name: meeting5-chat
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: BACKEND_URL
        sync: false
      - key: BACKEND_PRINCIPAL_URL
        sync: false  
      - key: FRONTEND_URL
        sync: false  
